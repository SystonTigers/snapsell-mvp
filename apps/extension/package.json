{
  "name": "snapsell-extension",
  "private": true,
  "scripts": {
    "build": "echo Build step TBD",
    "lint": "eslint --ext .ts src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  }
}
